/**
 * Point d'entrée pour les types de stores
 */

// Export d'un type vide pour que le fichier soit considéré comme un module
export type StoreTypes = 'userStore' | 'workspaceStore' | 'roomStore';

// Note: Les interfaces des stores sont définies dans leurs fichiers respectifs
// et sont exposées via le module declarations.d.ts
